"""
Recall API — AI-Powered Social Media Knowledge Manager
FastAPI Backend with Supabase + instructor (9router)

Quick start:
    1. cp .env.example .env  # fill in your credentials
    2. pip install -e .     # or: pip install -e ".[dev]"
    3. Run migrations in Supabase SQL Editor (migrations/001_*.sql → 003_*.sql)
    4. uvicorn app.main:app --reload --port 8000
"""
__version__ = "0.1.0"